# 安全与内容保护说明

这个项目是公开可访问的静态公益站点，但内容更新并不是公开可写的。

## 站点安全边界

- 公开访问者只能浏览页面，不能直接修改线上内容。
- 线上内容只会通过仓库提交、合并和 GitHub Pages 部署更新。
- 任何抓取、案例整理、趋势研判结果都应视为“站内参考”，不等同于警方定性或法律意见。

## 隐私与敏感信息原则

- 不收集、不存储报案人的真实隐私数据到服务器。
- 报案材料整理工具仅用于本地页面内整理和复制，不会主动上传内容。
- 提交真实案例时，应删除或模糊以下信息：
  - 身份证号
  - 完整手机号
  - 完整银行卡号
  - 详细家庭住址
  - 人脸、病历、验证码、支付密码等高敏感信息

## 协作与防破坏建议

建议在 GitHub 仓库后台启用以下设置：

1. 保护 `main` 分支。
2. 禁止直接推送到 `main`，统一通过 PR 合并。
3. 启用 “Require a pull request before merging”。
4. 启用 “Require status checks to pass before merging”。
5. 将写权限只保留给可信维护者。
6. 如无精力维护公开讨论区，可限制或关闭不必要的 Discussions。

## 内容审核建议

- 自动抓取结果先作为待审核材料，不直接视为权威结论。
- 新增案例至少核对：
  - 来源是否公开可验证
  - 是否包含个人隐私
  - 标题、诈骗类型、时间、区域是否明显错误
  - 是否夸大、误导或带有营销链接

## 漏洞与问题反馈

如果你发现的是以下问题，请不要直接公开贴出完整细节：

- 可导致站点被篡改的配置问题
- 泄露隐私的页面内容
- 恶意链接、钓鱼链接、脚本注入
- 报案整理工具中可能误导用户泄露更多敏感信息的设计

建议通过仓库维护者可见的方式联系，或先提交不含敏感细节的 issue。

## 不在本仓库范围内的内容

- 警方侦查判断
- 法律结论
- 资金追回承诺
- 真实涉案人员定位与身份确认

本项目可以做“线索整理、风险提示、内容归档”，但不能替代正式报案、止付和线下侦查。

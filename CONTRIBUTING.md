# 贡献说明

这个项目优先追求内容清晰、构建稳定、维护简单。欢迎任何小而实用的改进。

## 适合提交的内容

- 修正文案、错别字、失效链接
- 补充诈骗场景和家庭防骗建议
- 提交公开可核实的真实案例
- 改进抓取脚本、发布流程和文档构建稳定性

## 提交前建议

1. 保持改动尽量小，单次只解决一类问题
2. 避免引入复杂依赖或重型后端
3. 本地先运行一次：

```bash
npm run check
```

如果涉及 Python 抓取逻辑，也建议运行：

```bash
python -m compileall scripts
```

## 内容原则

- 优先使用通俗中文，避免术语堆积
- 面向普通家庭，强调可执行建议
- 不夸大、不制造恐慌
- 尽量引用公开可核实来源
- 涉及真实案例时，注意删除个人隐私信息

## 发布方式

仓库已内置自动化流程，推荐使用：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\publish.ps1 -Message "更新说明"
```

这条命令会自动构建、提交、推送，随后由 GitHub Actions 自动部署。

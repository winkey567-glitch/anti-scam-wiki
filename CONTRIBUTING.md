# 贡献说明

这个项目优先追求内容清晰、构建稳定、维护简单。欢迎任何小而实用的改进。

## 适合提交的内容

- 修正文案、错别字、失效链接
- 补充诈骗场景和家庭防骗建议
- 提交公开可核实的真实案例
- 改进抓取脚本、发布流程和文档构建稳定性

## 提交前建议

1. 保持改动尽量小，单次只解决一类问题。
2. 避免引入复杂依赖或重型后端。
3. 本地先运行：

```bash
npm run check
```

如果涉及 Python 抓取逻辑，也建议运行：

```bash
python -m compileall scripts
```

## 内容原则

- 优先使用通俗中文，避免术语堆积
- 面向普通家庭，强调可执行建议
- 不夸大、不制造恐慌
- 尽量引用公开可核实来源
- 涉及真实案例时，注意删除个人隐私信息

## 安全与隐私边界

- 不提交身份证号、完整手机号、完整银行卡号、验证码、人脸资料等敏感信息
- 不把站内趋势研判写成警方定案
- 不新增营销链接、下载引流或来源不明的外链
- 自动抓取内容进入仓库前，优先检查时间、区域、诈骗类型是否明显错误

详细说明见：[SECURITY.md](/G:/anti-scam-wiki/SECURITY.md)

## 推荐发布方式

仓库已经内置自动化脚本，推荐使用：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\publish.ps1 -Message "更新说明"
```

这条命令会自动构建、提交、推送，并交给 GitHub Actions 完成部署。
